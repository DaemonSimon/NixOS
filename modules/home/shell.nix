# modules/home/shell.nix
# Paper for terminal and small tools
{ config, pkgs, ... }:
{
  # Example: if you want your terminal settings managed by home-manager
  # programs.kitty.enable = true;
  # programs.bash.enable = true;

  # Keep empty for now - your existing ~/.config/kitty etc. stays as-is
}
